# Copy this file to .env and set your own passwords before starting.
#
#   cp .env.example .env
#
# The .env file is gitignored and never committed.

# Kasm noVNC web interface credentials
VNC_PW=changeme
VNC_VIEW_ONLY_PW=changeme-viewonly
